spring.application.name=BandBookingApplication

# ===============================
# = DATA SOURCE
# ===============================
spring.datasource.url=jdbc:postgresql://localhost:5432/bandbooking
spring.datasource.username=jspederson
spring.datasource.password=mSth67!;lPQ
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# = JPA / HIBERNATE
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Ensures Hibernate uses the correct timezone for ZonedDateTime
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# ===============================
# = JACKSON (ZonedDateTime Support)
# ===============================
spring.jackson.time-zone=UTC

# ===============================
# = SERVER CONFIG
# ===============================
server.port=8080
spring.web.error.include-message=always

# ===============================
# = LOGGING (Great for debugging availability queries)
# ===============================
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.pattern.console=%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level [%thread] [%X{traceId}] %logger - %msg%n

